name = "unacowa/mosura"

version = "0.1.0"

repository = "https://github.com/unacowa/mosura"

license = "Apache-2.0"

keywords = [ "graphql", "hasura", "postgres" ]

description = "A GraphQL engine (Hasura V3 reimplementation) in MoonBit"

source = "src"

preferred_target = "native"

import {
  "moonbitlang/async@0.20.2",
  "moonbit-community/postgres@0.0.6",
  "moonbit-community/yaml@0.0.6",
  "gmlewis/sha256@0.17.32",
}
